import { environment } from "../../environments/environment.development";

export const URL_BACKEND   = environment.URL_BACKEND;
export const URL_SERVICIOS = environment.URL_SERVICIOS;
export const URL_FRONTED   = environment.URL_FRONTEND;
export const PUBLIC_KEY    = environment.PUBLIC_KEY;
export const TEST_EPAYCO   = environment.TEST_EPAYCO;